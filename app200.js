let options = {
    strings:['Mening ishim IT dasturlash', "Biz bilan birga bo'ling"],
    typeSpeed: 40,
    startDelay: 1000,
    backSpeed:80,
    backDelay: 5000,
    loop:true,
    loopCount:Infinity,
};
let typed = new Typed('.element', options);